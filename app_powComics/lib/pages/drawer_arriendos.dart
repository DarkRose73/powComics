import 'package:flutter/material.dart';

class DrawerArriendos extends StatelessWidget {
  const DrawerArriendos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Arriendos'),
      ),
    );
  }
}
