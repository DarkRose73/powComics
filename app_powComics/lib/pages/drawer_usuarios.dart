import 'package:flutter/material.dart';

class DrawerUsuarios extends StatelessWidget {
  const DrawerUsuarios({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Usuarios'),
      ),
    );
  }
}
